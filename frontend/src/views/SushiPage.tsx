import FlexGrid from '../views/grids/FlexGrid';

const SushiPage: React.FC = () => {
    return (
        <div className="container mx-auto">
            <section className="py-8">
                <FlexGrid />
            </section>
        </div>
    );
};

export default SushiPage;